LISP := browse-kill-ring.el \
	greedy-delete.el \
	ksh-mode.el \
	libc-mode.el \
	s.el \
	ws-trim.el

# This is from gnus but is needed by various packages (e.g. tramp).
# This may cause problems if you have gnus installed.
#LISP += time-date.el

include ../Rules.make
