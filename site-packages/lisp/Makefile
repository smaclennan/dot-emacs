DIRS := sam misc anti-gnus
ifneq ($(EMACS),emacs)
DIRS += introspector
endif

all:
	@for d in $(DIRS); do \
		$(MAKE) -C $$d $(MFLAGS) all; \
	done

clean:
	@for d in $(DIRS); do \
		$(MAKE) -C $$d $(MFLAGS) clean; \
	done
