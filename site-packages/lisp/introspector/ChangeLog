2008-03-09  Stephen J. Turnbull  <stephen@xemacs.org>

	* Makefile (VERSION): Bump to 0.80.

	* Makefile:
	* introspector.el:
	* frame-utils.el:
	Fix up permissions notice.

	* introspector.el:
	* frame-utils.el:
	Update TODOs.

	* frame-utils.el: lisp-mnt conformance.

	* introspector.el (extent-introspector):
	Fix display of non-field, non-text-property extents.

2007-07-24  Stephen J. Turnbull  <stephen@xemacs.org>

	* introspector.el (ichar-introspector): Refactor arg cleanup.
	Eliminate local `atts', use arg `attributes' instead throughout.
	(introspector-maybe-format-simple-attribute): New macro.
	(ichar-introspector): Use it.

	(introspector-format-one-attribute): Remove comment.
	(introspector-ichar-default-attributes): Specify type in docstring.
	(ichar-introspector): Remove description of unimplement feature
	from docstring.

2007-07-22  Stephen J. Turnbull  <stephen@xemacs.org>

	* introspector.el (ichar-introspector): Improve docstring.
	(ichar-introspector): Use sequencing instead of a huge if.
	(ichar-introspector): Move check for Unicode support out of cond.
	(buffer-position-introspector-default-specification): Fix name of
	`introspect-cursor-position' in docstring.

2007-07-22  Stephen J. Turnbull  <stephen@xemacs.org>

	* introspector.el (introspector face): Comment on non-use.
	(introspector-format-one-attribute): Remove unneeded with-boundp.
	(ichar-introspector): Fix ichar description code and warning.
	(extent-introspector): Fix spacing in multiple face case.
	(introspect-display-frame): Move frame-specific face settings near
	lazy frame creation code.
	(introspect-display-frame): Various geometry hacks for 21.4
	compatibility.
	(introspect-display-frame): Use device-pixel-* functions, not
	display-pixel-*, for 21.4 compatibility.

2007-07-09  Stephen J. Turnbull  <stephen@xemacs.org>

	* introspector.el (introspect-cursor-position): New command.
	(help-map): Map ?= to introspect-cursor-position.

	* introspector.el (buffer-position-introspector):
	Renamed from cursor-position-introspector.
	Handle argument (was ignored).
	
	* introspector.el (buffer-position-introspector-default-specification):
	Renamed from introspector-cursor-position-default-attributes.
	Improve docstring.
	Fix initial value to correspond to default for &rest argument.

	* introspector.el (introspect-display-frame):
	Renamed from introspect.

2007-07-06  Stephen J. Turnbull  <stephen@xemacs.org>

	* introspector.el: Various small improvements.

2007-03-09  Stephen J. Turnbull  <stephen@xemacs.org>

	* .gitignore: New file.

	* introspector.el (introspector-format-one-attribute): New macro.
	(extent-introspector): Refactor to use it.

	(introspect): New, factored out of cursor-position-introspector.
	(cursor-position-introspector): Respect introspector API.

2007-03-08  Stephen J. Turnbull  <stephen@xemacs.org>

	* introspector.el: lisp-mnt conformance.

2006-08-12  Stephen J. Turnbull  <stephen@xemacs.org>

	* introspector.el:
	* frame-util.el:
	New files, broken out of author's init.el.

