# This is from gnus but is needed by various packages (e.g. tramp).
# This may cause problems if you have gnus installed.
# It causes huge problems with `emacs -nw'
LISP :=  time-date.el

# Introspector
LISP += frame-utils.el introspector.el

include ../Rules.make
