#!/bin/sh

[ -n "$1" ] || { echo "2emacs what?"; exit 1; }

N=`echo $1 | awk -F: '{print $2}'`
if [ -n "$N" ]; then
    FILE=`echo $1 | cut -d: -f1`
    ARG="+$N"
else
    FILE=$1
    unset ARG
fi

emacsclient -n $ARG $FILE
